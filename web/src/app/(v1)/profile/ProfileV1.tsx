// V1 profile — placeholder for the v1 user-facing profile page.
// Rendered inside V1Layout by (v2)/profile/page.tsx for non-admin users.
export default function ProfileV1() {
  return (
    <div style={{ maxWidth: "640px", margin: "0 auto", padding: "80px 24px 0", textAlign: "center" }}>
      <div style={{ fontSize: "22px", fontWeight: 700, color: "#1a1a1a", marginBottom: "12px" }}>
        Coming soon
      </div>
      <div style={{ fontSize: "14px", color: "#888", lineHeight: 1.6 }}>
        Profile settings are being prepared.
      </div>
    </div>
  );
}
